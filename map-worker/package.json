{
  "name": "map-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "esbuild src/index.ts --bundle --minify --platform=neutral --target=es2022 --outfile=dist/index.js"
  },
  "dependencies": {
    "pmtiles": "^4.4.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241022.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.3"
  }
}
